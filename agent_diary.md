# Дневник агента

## 2026-07-27 — автоматический каталог и релиз

- Подтянут `main` до `5b1a95f`.
- Источником данных сделаны папки `wallpapers/<id>/` и `manifest.json`.
- Добавлен генератор `scripts/generate_index.py`.
- PR теперь проверяется с автоматически собранным индексом.
- После merge GitHub обновляет `index.json`, ZIP изменённых обоев и release `bundles`.
- Все `downloadUrl` принудительно ведут на release этого репозитория.
- Неизменённые ZIP больше не перезаливаются, чтобы не сбрасывать их счётчики скачиваний.

## 2026-07-27 — удаления и защита PR

- Удалены `m3-shapes-gyro` и `m3-shapes-touch`.
- При удалении папки соответствующий ZIP теперь удаляется из release `bundles`.
- PR может менять только файлы внутри `wallpapers/<id>/`.

## 2026-07-28 — автоматика только в основном репозитории

- Workflow проверки и публикации запускают jobs только в `YouRooni/Wallify-Repo`.
- В форках генератор больше не коммитит `index.json`.
- PR из форков по-прежнему проверяются после отправки в основной репозиторий.
## 2026-07-30 — свободные имена комплектов

- Проверка PR теперь разрешает любые файлы и подпапки внутри `wallpapers/`.
- Валидатор и генератор каталога принимают имена вроде `DVD-Bouncers` и обложки с расширением `.jpeg`.

## 2026-07-30 — DVD-Bouncers в каталоге

- Исправлена ссылка на обложку `cover.jpeg` в манифесте и сгенерирована запись комплекта в `index.json`.
